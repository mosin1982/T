services:
  t-demo:
    build: .
    command: python t_cli.py demo

  t-real-binance:
    build: .
    command: python t_cli.py real-binance --symbol ${BINANCE_SYMBOL:-btcusdt} --threshold ${Z_SCORE_THRESHOLD:-3.0}
    env_file:
      - .env
    profiles:
      - real
